function week(input){
    let days = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"]
    let index = days.indexOf(input);
    if(index == -1){
       console.log("error");
    }
    else{
        console.log(index+1);
    }
}
